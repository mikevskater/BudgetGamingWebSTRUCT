INSERT INTO ssd 
        (
         ssd_formFactor, 
         ssd_size,
         ssd_interface,
         ssd_pcPart_id,
         ssd_pcBuild_id
         ) 
        VALUES (
                ?,
                ?,
                ?,
                ?,
                ?
                )